language: python

python:
  - "pypy"
  - "2.7"
  - "3.3"
  - "3.4"

install:
  - pip install -r requirements.txt
  - pip install -r test_requirements.txt
  - python setup.py install

script:
  - coverage run --source="bunch" -m bunch.test

after_success:
  - coveralls
